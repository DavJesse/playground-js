const id = (a) => a; // Returns the argument taken
const getLength = (b) => b.length; // Returns length of array or string

// //Print to console
// console.log(id('str'))
// console.log(getLength(['string', 'length']))